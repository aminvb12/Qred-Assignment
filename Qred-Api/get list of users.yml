info:
  name: get list of users
  type: http
  seq: 4

http:
  method: GET
  url: "{{base-url}}/users"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
