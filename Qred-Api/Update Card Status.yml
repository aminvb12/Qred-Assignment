info:
  name: Update Card Status
  type: http
  seq: 8

http:
  method: PATCH
  url: "{{base-url}}/cards/:cardId/status"
  params:
    - name: cardId
      value: f47f0ff2-9d0c-4fd7-8abc-1988b46a0ac0
      type: path
  body:
    type: json
    data: |-
      {
        "status": "active"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
