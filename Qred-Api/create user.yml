info:
  name: create user
  type: http
  seq: 1

http:
  method: POST
  url: "{{base-url}}/users"
  body:
    type: json
    data: |-
      {
        "first_name": "Amin",
        "last_name": "Kazemi",
        "email": "aminkazemi.kzm12@gmail.com",
        "personal_number": "199705313519"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
